<%= form_with(model: descriptor, url: descriptor_path(descriptor)) do |form| %>
  <div class="form-group mb-0">
    <%= form.number_field :sort_order,
                          class: 'form-control form-control-sm',
                          onchange: "this.form.submit();" %>
  </div>
<% end %>