<nav class="navbar navbar-expand-md navbar-dark bg-dark">
  <a class="navbar-brand text-white" href="<%= root_url %>"><%= fa_icon "cutlery", text: "Food Journal" %></a>
  <button class="navbar-toggler"
          type="button"
          data-toggle="collapse"
          data-target="#journal-nav-content"
          aria-controls="journal-nav-content"
          aria-expanded="false"
          aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="journal-nav-content">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item<%= ' active' if controller_name == 'journal' && action_name == 'index'  %>">
        <%= link_to fa_icon("home", text: '<span class="d-md-none">Home</span>'.html_safe, class: "fa-fw"), root_url, { class: "nav-link" } %>
      </li>
      <li class="nav-item<%= ' active' if controller_name == 'admin' && action_name == 'index'  %>">
        <%= link_to fa_icon("cogs", text: '<span class="d-md-none">Administration</span>'.html_safe, class: "fa-fw"), admin_url, { class: "nav-link" } %>
      </li>
    </ul>
  </div>
</nav>